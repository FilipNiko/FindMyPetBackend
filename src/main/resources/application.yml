server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/findmypet?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

application:
  security:
    jwt:
      secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
      expiration: 60000 # 1 minute
      refresh-token:
        expiration: 604800000 # 7 days

logging:
  level:
    root: INFO
    com.spring.findmypet: DEBUG
    org.springframework.security: DEBUG 